# Superseded: first end-to-end variability run

This is the first run of `experiments/e2evar`. It is kept only as a record. In this design the app was not pinned to one vCPU, so the client VM, not the app, limited throughput. The numbers therefore do not measure differences between app VMs. The corrected runs are `../e2evar-20260925-090121` and `../e2evar-20260925-092024-rep`.
